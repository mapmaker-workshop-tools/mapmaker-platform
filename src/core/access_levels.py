
"""def validate_user_access_to_card(request):
    card = Card.objects.get(id=id)
    if request.user in card.workshop.participants.all():
        print(True)
    else:
        print(False)
        
        
def validate_user_access_to_workshop(request):
    workshop = Workshop.objects.get(id=id)
    if request.user in workshop.participants.all():
        print(True)
    else:
        print(False)
        
def validate_user_workshop_owner(request):
    workshop = Workshop.objects.get(id=id)
    if request.user = workshop.workshop_owner:
        print(True)
    else:
        print(False)"""