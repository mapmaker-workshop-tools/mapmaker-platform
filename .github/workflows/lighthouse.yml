name: 💡 Google Lighthouse Check

on:
  push:
    branches:
      - '**'        # matches every branch

jobs:
  lighthouse:
    name: 💡 Lighthouse
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Lighthouse
      uses: foo-software/lighthouse-check-action@master
      with:
        urls: 'https://mapmaker.nl'